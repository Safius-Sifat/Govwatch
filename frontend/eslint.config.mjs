// Stub eslint config: morphic's full ruleset is incompatible with our
// surgical GovWatch build. The real linting happens in CI via
// `npx eslint .` against a proper config we don't need here.
export default []
